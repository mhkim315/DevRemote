# A1.2 C0D-R3 — Deferred Tool Research Evidence Report (Final)

Status: **C0D PROCEED — all 6 REJECT blockers on R2 (44d902b) remediated.**

Date: 2026-07-16. Executor: A1.2 execution agent. Provider: Claude Code 2.1.209.

## 1. R2 blocker closure

| # | Blocker | R3 fix |
|---|---------|--------|
| 1 | Session binding removed | `--fork-session` removed. Same-session `--resume` keeps session_id identical. Session_id comparison re-enabled: full identity = session_id + tool_use_id + tool_name + input_sha256. |
| 2 | Input digest truncated to 16 hex | Full 64-char SHA-256 now used everywhere (deferred_full.txt line 4, comparison in hook_resume.sh). |
| 3 | Decision fail-open | Closed vocabulary: exact `allow` or `deny` only. Unset, typo, unknown value → exit 2 (UNKNOWN_DECISION marker). Proven by wrong-decision trace. |
| 4 | Atomic claim scope insufficient | Claim directory shared at `/tmp/c0d-r3-claim/<tool_use_id>`. Any concurrent/fork resume sees the same directory → REPLAY_BLOCKED. Proven by replay trace. |
| 5 | Negative evidence not committed | 8 traces committed below, each with life.txt + deferred_identity.json + posttool_identity.json where applicable. |
| 6 | Session-mismatch markers from old hook | All markers now generated by the committed R3 hook. Session comparison is active; mismatch markers prove the current hook's behavior. |

## 2. Evidence matrix

| Trace | Life markers | PostToolUse | Probe | Verdict |
|---|---|---|---|---|
| allow | MATCH_OK(sid+tuid+tname+inhash) | ✅ matching tuid, is_error:false | EXISTS | Command executed |
| deny | MATCH_OK + RESEARCH_DENIAL | ❌ (never ran) | ABSENT | Denied |
| replay | MATCH_OK (1st) + REPLAY_BLOCKED (2nd, exit 2) | — | — | Atomic claim works |
| modified-input | MISMATCH input_sha256 + IDENTITY_MISMATCH_DENY | ❌ | — | Changed input blocked |
| wrong-tuid | MISMATCH tuid + IDENTITY_MISMATCH_DENY | ❌ | — | Wrong ID blocked |
| crash-malformed | MISMATCH (garbled deferred) | ❌ | — | Parse failure blocked |
| wrong-decision | MATCH_OK + UNKNOWN_DECISION(exit 2) | ❌ | — | Closed vocab enforced |
| exit-mid | MATCH_OK + RESEARCH_DENIAL + EXIT_MID | ❌ | ABSENT | Kill mid-resume safe |

## 3. Verdict

**C0D PROCEED** — exact same-session identity binding (session_id + tool_use_id + tool_name + full 64-char input_sha256), closed decision vocabulary, shared-scope atomic one-shot claim, fail-closed on mismatch/replay/malformed/unknown-decision. Allow produces PostToolUse with matching tool_use_id. All 8 mandatory negative traces committed.

C0D PROCEED only authorizes the reviewer to write a new implementation plan.
