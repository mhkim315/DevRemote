{
  "_manifest": "R2 fixture inventory",
  "_retrieval_date": "2026-07-12",
  "_protocol_generation": "ACP v1",
  "fixtures": [
    {
      "file": "goose-acp-protocol.json",
      "provider": "Goose (ACP — Agent Client Protocol)",
      "protocol_generation": "ACP v1",
      "pinned_schema_release": "schema-v1.19.0 (2026-07-06)",
      "pinned_schema_file": "schema/v1/schema.json",
      "pinned_schema_permalink": "https://raw.githubusercontent.com/agentclientprotocol/agent-client-protocol/schema-v1.19.0/schema/v1/schema.json",
      "local_schema_copy": "acp-v1-schema.json",
      "validation_script": "validate_acp.py",
      "source_type": "generated from pinned ACP v1 JSON Schema (schema-v1.19.0). All wire field names, discriminators, enum values, and required fields match the schema.",
      "wire_format": "camelCase JSON (optionId, toolCallId, sessionId, outcome discriminator)",
      "license": "Apache-2.0",
      "status": "available — schema-validated",
      "supports": "structured session lifecycle, tool call state machine, dedicated permission request/result protocol with typed PermissionOption (optionId/name/kind) and RequestPermissionOutcome (selected/cancelled), StopReason (end_turn/max_tokens/max_turn_requests/refusal/cancelled)",
      "schema_validated": true
    },
    {
      "file": "gemini-cli-hooks.json",
      "provider": "Gemini CLI",
      "pinned_commit": "e09410b6 (2026-04-10)",
      "pinned_file": "docs/hooks/reference.md",
      "source_type": "derived from official Gemini CLI hooks documentation at pinned commit",
      "license": "Apache-2.0",
      "status": "available — derived from docs at pinned commit",
      "supports": "session lifecycle events, policy/validation hooks (NOT approval)",
      "explicitly_absent": "no dedicated permission request/result event; BeforeTool is policy hook; Notification hook cannot block or grant permissions"
    },
    {
      "file": "acp-v1-schema.json",
      "description": "Pinned ACP v1 JSON Schema (schema-v1.19.0) used for fixture validation",
      "source": "https://raw.githubusercontent.com/agentclientprotocol/agent-client-protocol/schema-v1.19.0/schema/v1/schema.json",
      "license": "Apache-2.0"
    }
  ],
  "unavailable_fixtures": [
    {"provider": "OpenCode", "reason": "No official versioned SSE event schema with reproducible JSON examples found."},
    {"provider": "Cline", "reason": "Hook documentation at docs.cline.bot references v3.36+ but no pinned version, JSON schema, or reproducible test fixtures found."},
    {"provider": "OpenHands", "reason": "Hooks documented at docs.openhands.dev but under SDK restructuring. No versioned hook schema specification found."},
    {"provider": "Aider", "reason": "No structured event protocol exists. .aider.chat.history.md is a markdown conversation log, not an event protocol."}
  ]
}
