{
  "_manifest": "R2 fixture inventory",
  "_retrieval_date": "2026-07-12",
  "fixtures": [
    {
      "file": "goose-acp-protocol.json",
      "provider": "Goose (ACP — Agent Client Protocol)",
      "wire_protocol_version": "1",
      "schema_artifact": "agent-client-protocol-schema 1.4.0 (docs.rs)",
      "source_type": "derived from official ACP v1 schema struct/enum definitions at docs.rs",
      "license": "Apache-2.0",
      "status": "available — derived from schema",
      "supports": "structured session lifecycle, tool call state machine, dedicated permission request/result protocol with typed PermissionOption and RequestPermissionOutcome",
      "corrected_from_prior": "PermissionOption fields (option_id/name/kind) now match schema; outcome uses Selected/Cancelled variants; StopReason values corrected; wire protocol version 1 separated from artifact versions"
    },
    {
      "file": "gemini-cli-hooks.json",
      "provider": "Gemini CLI",
      "pinned_commit": "e09410b6 (2026-04-10)",
      "pinned_file": "docs/hooks/reference.md",
      "source_type": "derived from official Gemini CLI hooks documentation at pinned commit",
      "license": "Apache-2.0",
      "status": "available — derived from docs at pinned commit",
      "supports": "session lifecycle events, policy/validation hooks (NOT approval)",
      "explicitly_absent": "no dedicated permission request/result event; BeforeTool is policy hook; Notification hook cannot block or grant permissions"
    }
  ],
  "unavailable_fixtures": [
    {
      "provider": "OpenCode",
      "reason": "No official versioned SSE event schema with reproducible JSON examples found. Event types documented in plugin system but not as a versioned protocol specification with payload schemas."
    },
    {
      "provider": "Cline",
      "reason": "Hook documentation at docs.cline.bot references v3.36+ but no pinned version, JSON schema, or reproducible test fixtures found."
    },
    {
      "provider": "OpenHands",
      "reason": "Hooks documented at docs.openhands.dev but under SDK restructuring. No versioned hook schema specification or reproducible fixtures found."
    },
    {
      "provider": "Aider",
      "reason": "No structured event protocol exists. .aider.chat.history.md is a markdown conversation log, not an event protocol. Official docs confirm this is a chat history file only."
    }
  ]
}
