{
  "_provenance": {
    "provider": "Goose (ACP — Agent Client Protocol)",
    "protocol_generation": "ACP v1",
    "pinned_source": {
      "repository": "agentclientprotocol/agent-client-protocol",
      "release_tag": "schema-v1.19.0",
      "schema_file": "schema/v1/schema.json",
      "schema_commit": "schema-v1.19.0 (GitHub release, 2026-07-06)",
      "schema_permalink": "https://raw.githubusercontent.com/agentclientprotocol/agent-client-protocol/schema-v1.19.0/schema/v1/schema.json",
      "local_copy": "docs/r2-fixtures/acp-v1-schema.json"
    },
    "retrieval_date": "2026-07-12",
    "license": "Apache-2.0 (agent-client-protocol)",
    "capture_method": "generated from pinned ACP v1 JSON Schema (schema-v1.19.0). All field names, discriminators, enum values, and required fields match the schema exactly. JSON examples are minimal valid instances of each schema type.",
    "redactions": ["<SESSION_ID>", "<CWD>", "<TOOL_CALL_ID>"],
    "_note": "ACP wire compatibility is determined by initialize.protocolVersion negotiation (current stable: 1). Schema artifact version 1.19.0 is the release tag; it describes protocol generation v1 types. Crate versions (0.1.1, 1.4.0) are separate SDK artifacts and NOT wire protocol versions."
  },

  "_wire_traceability": {
    "_description": "Mapping from Rust/SDK type names to actual ACP v1 wire JSON",
    "mappings": [
      {
        "rust_type": "agent_client_protocol_schema::v1::PermissionOption",
        "rust_member": "option_id: PermissionOptionId",
        "wire_json": "optionId (string)",
        "schema_location": "#/$defs/PermissionOption",
        "fixture_path": "examples.permission_option.example"
      },
      {
        "rust_type": "agent_client_protocol_schema::v1::PermissionOption",
        "rust_member": "name: String",
        "wire_json": "name (string)",
        "schema_location": "#/$defs/PermissionOption",
        "fixture_path": "examples.permission_option.example"
      },
      {
        "rust_type": "agent_client_protocol_schema::v1::PermissionOption",
        "rust_member": "kind: PermissionOptionKind",
        "wire_json": "kind (string: allow_once | allow_always | reject_once | reject_always)",
        "schema_location": "#/$defs/PermissionOptionKind",
        "fixture_path": "examples.permission_option.example"
      },
      {
        "rust_type": "agent_client_protocol_schema::v1::ToolCallUpdate",
        "rust_member": "tool_call_id: ToolCallId",
        "wire_json": "toolCallId (string)",
        "schema_location": "#/$defs/ToolCallUpdate",
        "fixture_path": "examples.request_permission.request.params.toolCall"
      },
      {
        "rust_type": "agent_client_protocol_schema::v1::RequestPermissionOutcome",
        "rust_member": "Cancelled",
        "wire_json": "{\"outcome\": \"cancelled\"}",
        "schema_location": "#/$defs/RequestPermissionOutcome",
        "fixture_path": "examples.request_permission.response_cancelled"
      },
      {
        "rust_type": "agent_client_protocol_schema::v1::RequestPermissionOutcome",
        "rust_member": "Selected(SelectedPermissionOutcome)",
        "wire_json": "{\"outcome\": \"selected\", \"optionId\": \"...\"}",
        "schema_location": "#/$defs/RequestPermissionOutcome + #/$defs/SelectedPermissionOutcome",
        "fixture_path": "examples.request_permission.response_selected"
      },
      {
        "rust_type": "agent_client_protocol_schema::v1::StopReason",
        "rust_member": "EndTurn",
        "wire_json": "\"end_turn\"",
        "schema_location": "#/$defs/StopReason",
        "fixture_path": "examples.stop_reason._values"
      },
      {
        "rust_type": "agent_client_protocol_schema::v1::SessionUpdate",
        "rust_member": "sessionUpdate discriminator",
        "wire_json": "sessionUpdate (string: user_message_chunk | agent_message_chunk | agent_thought_chunk | tool_call | tool_call_update | plan | ...)",
        "schema_location": "#/$defs/SessionUpdate",
        "fixture_path": "examples.session_update_tool_call.notification.params.update"
      }
    ]
  },

  "_evidence_classification": {
    "permission_option": "documented — from pinned ACP v1 schema (schema-v1.19.0, #/$defs/PermissionOption)",
    "request_permission_outcome": "documented — from pinned ACP v1 schema (#/$defs/RequestPermissionOutcome, discriminator: outcome)",
    "selected_permission_outcome": "documented — from pinned ACP v1 schema (#/$defs/SelectedPermissionOutcome, field: optionId)",
    "stop_reason": "documented — from pinned ACP v1 schema (#/$defs/StopReason, values: end_turn, max_tokens, max_turn_requests, refusal, cancelled)",
    "session_new": "inferred — JSON-RPC structure from ACP architecture docs; exact params not defined in v1 schema $defs",
    "session_update": "documented — from pinned ACP v1 schema (#/$defs/SessionUpdate, discriminator: sessionUpdate)",
    "tool_call_update": "documented — from pinned ACP v1 schema (#/$defs/ToolCallUpdate, required: toolCallId)"
  },

  "_conclusions_supported": [
    "ACP v1 defines wire types with camelCase JSON field names (schema-v1.19.0)",
    "PermissionOption wire format: {optionId, name, kind} — all required",
    "RequestPermissionOutcome discriminator: outcome = 'selected' | 'cancelled'",
    "SelectedPermissionOutcome carries optionId (the chosen PermissionOption.optionId)",
    "StopReason (ACP v1): end_turn, max_tokens, max_turn_requests, refusal, cancelled",
    "ToolCallUpdate requires toolCallId; carries status, kind, title, locations, rawInput, rawOutput",
    "SessionUpdate discriminator: sessionUpdate with 11 documented variants"
  ],

  "_conclusions_not_supported": [
    "This fixture does NOT prove Goose's implementation matches ACP v1 schema at any specific commit",
    "This fixture does NOT prove Pokit-session to Goose-session correlation (requires managed launch)",
    "This fixture does NOT prove ACP long-term wire stability",
    "ACP v1 schema does NOT define an 'error' StopReason value",
    "ACP v1 StopReason is NOT feature-gated — it is defined in the pinned v1 schema"
  ],

  "examples": {
    "session_new": {
      "_schema_source": "ACP architecture doc; session/new params not in v1 schema $defs",
      "request": {
        "jsonrpc": "2.0",
        "id": 2,
        "method": "session/new",
        "params": {
          "cwd": "<CWD>",
          "mcpServers": []
        }
      },
      "response": {
        "jsonrpc": "2.0",
        "id": 2,
        "result": {
          "sessionId": "<SESSION_ID>"
        }
      }
    },

    "permission_option": {
      "_schema_source": "#/$defs/PermissionOption (schema-v1.19.0)",
      "_schema_required": ["optionId", "name", "kind"],
      "example": {
        "optionId": "allow_once",
        "name": "Allow once",
        "kind": "allow_once"
      }
    },

    "request_permission": {
      "_schema_source": "#/$defs/RequestPermissionRequest (schema-v1.19.0)",
      "_schema_required": ["sessionId", "toolCall", "options"],
      "request": {
        "jsonrpc": "2.0",
        "id": 5,
        "method": "session/request_permission",
        "params": {
          "sessionId": "<SESSION_ID>",
          "toolCall": {
            "toolCallId": "<TOOL_CALL_ID>",
            "kind": "edit",
            "status": "awaiting_permission",
            "title": "Edit file"
          },
          "options": [
            {"optionId": "allow_once", "name": "Allow once", "kind": "allow_once"},
            {"optionId": "allow_always", "name": "Always allow", "kind": "allow_always"},
            {"optionId": "reject_once", "name": "Reject", "kind": "reject_once"}
          ]
        }
      },
      "response_selected": {
        "_schema_source": "#/$defs/RequestPermissionResponse + #/$defs/RequestPermissionOutcome (outcome='selected') + #/$defs/SelectedPermissionOutcome (schema-v1.19.0)",
        "_schema_required": ["outcome"],
        "jsonrpc": "2.0",
        "id": 5,
        "result": {
          "outcome": {
            "outcome": "selected",
            "optionId": "allow_once"
          }
        }
      },
      "response_cancelled": {
        "_schema_source": "#/$defs/RequestPermissionResponse + #/$defs/RequestPermissionOutcome (outcome='cancelled') (schema-v1.19.0)",
        "_schema_required": ["outcome"],
        "jsonrpc": "2.0",
        "id": 5,
        "result": {
          "outcome": {
            "outcome": "cancelled"
          }
        }
      }
    },

    "session_update_tool_call": {
      "_schema_source": "#/$defs/SessionUpdate + #/$defs/ToolCallUpdate (schema-v1.19.0)",
      "_tool_call_update_required": ["toolCallId"],
      "notification": {
        "jsonrpc": "2.0",
        "method": "session/update",
        "params": {
          "sessionId": "<SESSION_ID>",
          "update": {
            "sessionUpdate": "tool_call_update",
            "toolCallId": "<TOOL_CALL_ID>",
            "status": "running",
            "kind": "edit",
            "title": "Editing file"
          }
        }
      }
    },

    "stop_reason": {
      "_schema_source": "#/$defs/StopReason (schema-v1.19.0 — ACP v1, NOT v2/latest)",
      "_values": [
        "end_turn",
        "max_tokens",
        "max_turn_requests",
        "refusal",
        "cancelled"
      ],
      "_note": "ACP v1 StopReason is defined in the pinned schema-v1.19.0. There is NO 'error' value. Protocol/transport failures are separate from StopReason and must be handled as JSON-RPC errors or session error events.",
      "failure_handling": {
        "jsonrpc_error": "JSON-RPC error response (code + message) — transport/protocol level, not StopReason",
        "session_error": "session/update with error content — if documented by implementation",
        "process_failure": "Process exit / connection loss — runtime signal, not StopReason",
        "transport_failure": "stdio/HTTP disconnect — runtime signal, not StopReason"
      }
    }
  }
}
