{
  "_provenance": {
    "provider": "Goose (ACP — Agent Client Protocol)",
    "wire_protocol_version": "1",
    "schema_artifact_versions": {
      "agent-client-protocol": "0.1.1 (Rust SDK crate — artifact version, NOT wire protocol)",
      "agent-client-protocol-schema": "1.4.0 (schema crate — defines v1 wire types)"
    },
    "_note": "ACP wire compatibility is determined by initialize.protocolVersion negotiation, not by crate artifact versions. The wire protocol version is 1. Crate versions are SDK release artifacts and must not be used as protocol version gates.",
    "source_permalinks": [
      "https://docs.rs/agent-client-protocol-schema/1.4.0/agent_client_protocol_schema/v1/struct.PermissionOption.html",
      "https://docs.rs/agent-client-protocol-schema/1.4.0/agent_client_protocol_schema/v1/enum.RequestPermissionOutcome.html",
      "https://docs.rs/agent-client-protocol-schema/1.4.0/agent_client_protocol_schema/v1/struct.SelectedPermissionOutcome.html",
      "https://docs.rs/agent-client-protocol-schema/latest/agent_client_protocol_schema/v2/enum.StopReason.html",
      "https://docs.rs/agent-client-protocol-schema/1.4.0/agent_client_protocol_schema/v1/struct.RequestPermissionResponse.html",
      "https://github.com/agentclientprotocol/agent-client-protocol",
      "https://agentclientprotocol.com/get-started/architecture"
    ],
    "retrieval_date": "2026-07-12",
    "license": "Apache-2.0 (agent-client-protocol)",
    "capture_method": "derived from official ACP v1 schema definitions at docs.rs (1.4.0). JSON examples constructed from struct/enum field definitions, NOT copied from a pre-existing example file.",
    "redactions": ["<SESSION_ID>", "<CWD>", "<TOOL_CALL_ID>", "<FILE_PATH>"],
    "conclusions_supported": [
      "ACP v1 defines structured JSON-RPC 2.0 types for agent-client communication",
      "PermissionOption has required fields: option_id (PermissionOptionId), name (String), kind (PermissionOptionKind)",
      "RequestPermissionOutcome is Cancelled | Selected(SelectedPermissionOutcome)",
      "SelectedPermissionOutcome carries option_id (PermissionOptionId)",
      "StopReason v1: EndTurn, MaxTokens, MaxTurnRequests, Refusal, Cancelled, Other(String)",
      "session/new creates a session and returns sessionId",
      "session/request_permission is a dedicated bidirectional request for user approval"
    ],
    "conclusions_not_supported": [
      "This fixture does NOT prove Goose's current implementation matches ACP v1 schema exactly",
      "This fixture does NOT prove Pokit-session to Goose-session correlation (requires managed launch)",
      "This fixture does NOT prove ACP long-term wire stability (protocol evolution is governed by capability negotiation)",
      "ACP artifact versions (0.1.1, 1.4.0) are NOT wire protocol versions"
    ]
  },

  "_evidence_classification": {
    "permission_option_schema": "documented — from docs.rs/agent-client-protocol-schema/1.4.0 struct definition",
    "request_permission_outcome": "documented — from docs.rs enum definition",
    "selected_permission_outcome": "documented — from docs.rs struct definition",
    "stop_reason": "documented — from docs.rs enum definition (v2 feature-gated but v1 subset is EndTurn, MaxTokens, Cancelled)",
    "session_new_format": "inferred — JSON-RPC structure from ACP architecture docs; exact params from protocol specification",
    "session_update_tool_call": "inferred — notification structure from ACP architecture docs; exact content fields from schema"
  },

  "examples": {
    "session_new": {
      "_source": "ACP architecture doc (agentclientprotocol.com/get-started/architecture)",
      "request": {
        "jsonrpc": "2.0",
        "id": 2,
        "method": "session/new",
        "params": {
          "cwd": "<CWD>",
          "mcpServers": []
        }
      },
      "response": {
        "jsonrpc": "2.0",
        "id": 2,
        "result": {
          "sessionId": "<SESSION_ID>"
        }
      }
    },

    "permission_option": {
      "_source": "docs.rs/agent-client-protocol-schema/1.4.0/v1/struct.PermissionOption.html",
      "_schema": {
        "option_id": "<PermissionOptionId> — unique identifier",
        "name": "<String> — human-readable label",
        "kind": "<PermissionOptionKind> — hint about nature of option",
        "meta": "<Option<Meta>> — reserved, optional"
      },
      "example": {
        "option_id": "allow_once",
        "name": "Allow once",
        "kind": "allow_once"
      }
    },

    "request_permission": {
      "_source": "ACP architecture doc + PermissionOption/RequestPermissionResponse schema",
      "request": {
        "jsonrpc": "2.0",
        "id": 5,
        "method": "session/request_permission",
        "params": {
          "sessionId": "<SESSION_ID>",
          "toolCall": {
            "id": "<TOOL_CALL_ID>",
            "status": "awaiting_permission"
          },
          "options": [
            {"option_id": "allow_once", "name": "Allow once", "kind": "allow_once"},
            {"option_id": "allow_always", "name": "Always allow", "kind": "allow_always"},
            {"option_id": "deny", "name": "Deny", "kind": "deny"}
          ]
        }
      },
      "response_selected": {
        "_outcome_variant": "Selected",
        "jsonrpc": "2.0",
        "id": 5,
        "result": {
          "outcome": {
            "Selected": {
              "option_id": "allow_once"
            }
          }
        }
      },
      "response_cancelled": {
        "_outcome_variant": "Cancelled",
        "jsonrpc": "2.0",
        "id": 5,
        "result": {
          "outcome": "Cancelled"
        }
      }
    },

    "session_update_tool_call": {
      "_source": "ACP architecture doc (notification structure inferred from spec descriptions)",
      "notification": {
        "jsonrpc": "2.0",
        "method": "session/update",
        "params": {
          "sessionId": "<SESSION_ID>",
          "update": {
            "sessionUpdate": "tool_call_update",
            "content": {
              "id": "<TOOL_CALL_ID>",
              "status": "running"
            }
          }
        }
      }
    },

    "stop_reason": {
      "_source": "docs.rs/agent-client-protocol-schema/latest/v2/enum.StopReason.html",
      "_values": ["EndTurn", "MaxTokens", "MaxTurnRequests", "Refusal", "Cancelled"],
      "_other": "Other(String) — custom or future stop reason",
      "_note": "StopReason is feature-gated behind unstable_protocol_v2. v1 subset available values may differ. EndTurn, MaxTokens, and Cancelled are the most commonly documented."
    }
  }
}
